# URL of the FastAPI backend (e.g. your Hugging Face Space), no trailing slash.
BACKEND_URL=https://your-space.hf.space

# Shared bearer token the backend requires on every request (server-side only, never exposed to the browser).
BACKEND_TOKEN=change-me
