# backend/requirements.txt  (Phase 0 subset — grows per phase)
fastapi
uvicorn[standard]
pydantic>=2
httpx
pytest
pymupdf
pillow
torch          # install CPU build: pip install torch --index-url https://download.pytorch.org/whl/cpu
python-doctr   # NOTE: the [torch] extra was removed in 1.0.1; install plain, with torch already present
img2table      # table structure detection; uses its DocTR OCR backend (no system Tesseract)
pandas
tabulate       # powers pandas' df.to_markdown() used by app/ingest/tables.py
python-multipart  # required by FastAPI for multipart/form-data file uploads (POST /ingest)
datasets       # HF `datasets` lib; used by scripts/fetch_sample_docs.py (eval corpus), not a test dep
sentence-transformers  # bge text/CLIP image embeddings, bge-reranker, NLI (Phase 2+)
faiss-cpu      # in-process vector index (Task 2.2)
rank-bm25      # sparse retrieval for hybrid fusion (Task 2.2+)
numpy
